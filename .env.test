# Dedicated E2E test user (regular "user" role, NOT the admin account).
# Created automatically by e2e/global-setup.ts if it doesn't exist yet.
E2E_TEST_USER_NAME=E2E Test User
E2E_TEST_USER_EMAIL=e2e-user+drpass@localhost.test
E2E_TEST_USER_PASSWORD=E2eTestPass123!

# Backend auth endpoint (must match VITE_AUTH_API_URL in .env)
VITE_AUTH_API_URL=http://localhost:8080/api/auth
